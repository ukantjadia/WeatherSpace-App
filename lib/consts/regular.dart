import 'package:flutter/material.dart';
import 'package:velocity_x/velocity_x.dart';

double test(int percent, context)  {
   return context.percentWidth * percent;
}

double fun(context,double per){
  return context.percentWidth * per;
}